import 'filter_menu_panel_data.dart';

/// 移动端不用 HTML 菜单（返回空 id，不会用到）。
String registerFilterMenuPanel({
  required List<FilterMenuRowData> rows,
  required int surfaceArgb,
  required int outlineArgb,
  required int onSurfaceArgb,
}) {
  return '';
}
